'use strict';

const linkedList = require('../linked-list.js');

// let testVal = 0;
// const inputArr = [5, 6, 7, 8];
// const inputVal = 7;
// const goodIndex = 2;
// const badIndex = 3;

// describe('array module', () => {
//   it('should give the index of the matching element', () => {
//     testVal = binarySearch(inputArr, inputVal)
//     expect(testVal).toStrictEqual(goodIndex);
//   });
//   it('should fail if the index of the element and value don\'t match', () => {
//     testVal = binarySearch(inputArr, inputVal)
//     expect(testVal).not.toEqual(badIndex);
//   });
//   it('should fail if the input value is not a number', () => {
//     testVal = binarySearch(inputArr, inputVal)
//     expect(testVal).not.toBe(isNaN);
//   });
// });
